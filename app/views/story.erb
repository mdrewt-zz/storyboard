<div class="container"><br>

  <h2><%= @story.title %></h2>
  <h3>by <%= @story.user.name %></h3>
  <h4>Summary</h4>
  <p><%= @story.summary %></p>
  
</div>

<%#= erb :testing if @segment %>

<div id="segments" href=<%="/json/#{@segment.story_id}/#{@segment.id}/0"%>>
</div>


<div class="container create">
  <h3>Write something!</h3>
  <form action=<%="/story/#{@segment.id}/segment/#{@segment.id}/new"%> method="post">
    <br>
    <textarea rows="4" cols="40" name="body" placeholder="What happens next?"></textarea><br><br>
    <input type="submit" value="Submit">
  </form>
</div>